This could be done in console

const htmlBody = document.querySelector('body');

const randomClikFunction = function() {
  const color = ['red', 'green', 'blue', 'yellow', 'orange', 'purple'];
    
  const randomIndex = Math.floor(Math.random() * color.length);

  cosnt randomColor = color[randomIndex];

  htmlBody.style.backgroundColor = randomColor;

}

randomClikFunction(); //Color changes;

htmlBody.onclick = randomClikFunction; //When you click color automatically changes
